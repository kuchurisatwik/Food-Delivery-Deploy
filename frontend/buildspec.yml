version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing frontend deps"
      - cd frontend
      - npm ci
  build:
    commands:
      - echo "Building frontend"
      - npm run build
artifacts:
  base-directory: frontend/build
  files:
    - '**/*'
